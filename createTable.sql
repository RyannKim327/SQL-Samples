-- Create Table
CREATE TABLE myDatabase (
  ID INT(11) NOT NULL PRIMARY KEY,
  Name VARCHAR(100)
);

-- NOT NULL defines as this data must not be empty, or basically required to have value
-- VARCHAR and INT are just datatypes, the number inside of it are just the length or total of characters
